# Project Instructions

- This repo includes a local skill at `.agents/skills/refresh-codex-thread/SKILL.md`.
- When the user says `refresh thread`, or asks to refresh/open/select a Codex desktop thread, use that local skill and its scripts instead of inventing a new flow.
